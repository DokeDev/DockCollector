let current=null, tab='boards', poll=null, picked=null, stopPickerIndex=-1, resultsPage=1, resultsBusy=false, resultsSignature='', selectedRules=new Set(), selectedBoards=new Set(), selectedPickerSources=new Set();
const $=s=>document.querySelector(s), esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function api(url,opt={}){const r=await fetch(url,{headers:{'Content-Type':'application/json'},...opt});if(!r.ok)throw Error(await r.text());return r.json()}
async function loadTargets(){const xs=await api('/api/targets');$('#targets').innerHTML=xs.map(x=>{let active=current?.id===x.id;return `<div class="target-shell ${active?'active':''}"><button class="target ${active?'active':''}" data-target-id="${esc(x.id)}" onclick="loadTarget('${x.id}')"><b><i class="dot ${x.status.state}"></i>${esc(x.name)}</b><small>${esc(x.rule.domain)} · ${sidebarStateName(x.status.state)}</small></button>${active?`<div class="target-actions"><button onclick="renameCurrentTarget()">重命名</button><button class="delete" onclick="deleteCurrentTarget()">删除</button></div>`:''}</div>`}).join('');if(!current&&xs[0])loadTarget(xs[0].id)}
async function loadTarget(id){current=await api('/api/targets/'+id);resultsPage=1;resultsSignature='';selectedRules.clear();selectedBoards.clear();selectedPickerSources.clear();$('#title').textContent=current.name;$('#crumb').textContent=current.rule.domain;render();updateStatus(current.status);loadTargets()}
function stateName(s){return({idle:'空闲',starting:'启动中',running:'采集中',paused:'已暂停',stopping:'停止中',stopped:'已停止',completed:'已完成',error:'错误'})[s]||s}
function sidebarStateName(s){return s==='idle'?'任务空闲':stateName(s)}
async function refreshTargetStates(){let xs=await api('/api/targets'),buttons=[...document.querySelectorAll('#targets .target')];for(let button of buttons){let x=xs.find(v=>v.id===button.dataset.targetId);if(!x)continue;let dot=button.querySelector('.dot'),small=button.querySelector('small');if(dot)dot.className=`dot ${x.status.state}`;if(small)small.textContent=`${x.rule.domain} · ${sidebarStateName(x.status.state)}`}}
function updateStatus(s){$('#state').textContent=stateName(s.state);for(const k of ['pages','opened','saved','captcha'])$('#'+k).textContent=s[k]||0;$('#message').textContent=s.message||'—';let active=['starting','running','paused'].includes(s.state);$('#startBtn').disabled=active||s.state==='stopping';$('#pauseBtn').textContent=s.state==='paused'?'▶ 继续':'Ⅱ 暂停';$('#pauseBtn').disabled=!['running','paused'].includes(s.state);$('#stopBtn').disabled=!active}
$('#tabs').onclick=e=>{if(e.target.dataset.tab){tab=e.target.dataset.tab;document.querySelectorAll('nav button').forEach(x=>x.classList.toggle('active',x===e.target));render()}}
function render(){if(!current)return;const r=current.rule;let h='';if(tab==='boards')h=boards(r);if(tab==='filter')h=filter(r);if(tab==='fields')h=fields(r);if(tab==='limits')h=limits(r);if(tab==='browser')h=browserMode(r);if(tab==='proxy')h=proxy(r);if(tab==='results'){resultsSignature='';results(true)}else{$('#panel').innerHTML=h;decorateSelectionControls()}}
function boards(r){return `<div class="notice">这里每一行都是一条数据列表页。可以删除单条，也可以勾选多条后批量删除；删除后自动保存。</div><div class="card"><div class="card-head"><h2>数据列表页 · ${r.boards.length} 条</h2><div><button class="danger" onclick="deleteSelectedBoards()">删除所选</button><button onclick="addBoard()">＋ 添加列表页</button></div></div>${r.boards.map((b,i)=>`<div class="board source-rule"><input title="选择此规则" type="checkbox" ${selectedBoards.has(i)?'checked':''} onchange="toggleBoard(${i},this.checked)"><label class="enable-switch"><input type="checkbox" ${b.enabled?'checked':''} onchange="setBoard(${i},'enabled',this.checked)">启用</label><input value="${esc(b.name)}" onchange="setBoard(${i},'name',this.value)" placeholder="列表名称"><input value="${esc(b.url)}" onchange="setBoard(${i},'url',this.value)" placeholder="列表页地址"><input placeholder="独立代理；留空继承目标代理" value="${esc(b.proxy||'')}" onchange="setBoard(${i},'proxy',this.value)"><button class="danger source-delete" onclick="delBoard(${i})">删除此列表页</button></div>`).join('')||'<div class="empty-rules">暂无数据列表页，请点击“添加列表页”。</div>'}</div>`}
function filter(r){let x=r.list;return `<div class="card"><div class="card-head"><h2>列表定位与打开前过滤</h2></div><div class="card-body grid two"><label>帖子行 CSS 选择器<input value="${esc(x.row_selector)}" onchange="setPath('list.row_selector',this.value)"></label><label>详情链接 CSS 选择器<input value="${esc(x.link_selector)}" onchange="setPath('list.link_selector',this.value)"></label><label>列表时间 CSS 选择器<input value="${esc(x.time_selector||'')}" onchange="setPath('list.time_selector',this.value)"></label><label>时间必须包含的文本<input value="${esc(x.required_text)}" onchange="setPath('list.required_text',this.value)" placeholder="例如：前天、天前"></label><label>翻页方式<select onchange="setPath('list.pagination_mode',this.value)"><option value="next" ${x.pagination_mode!=='scroll'?'selected':''}>下一页按钮</option><option value="scroll" ${x.pagination_mode==='scroll'?'selected':''}>滚动到底加载</option></select></label><label>下一页 CSS 选择器<input value="${esc(x.next_selector)}" onchange="setPath('list.next_selector',this.value)" placeholder="滚动模式可留空"></label></div><p class="hint">只检查“列表时间 CSS 选择器”定位到的元素，并使用包含匹配。例如时间为“4 天前”，条件填“天前”即可命中。</p></div>${listExcludeRules(r)}`}

function listExcludeRules(r){let rules=r.list.exclude_rules||[];return `<div class="card"><div class="card-head"><h2>列表项过滤规则 · ${rules.length} 条</h2><div><button onclick="addListExcludeRule()">＋ 手动增加</button><button class="primary" onclick="startNewListExcludePicker()">打开浏览器 · 新增过滤规则</button></div></div>${rules.map((x,i)=>`<div class="stop-rule"><label class="enable-switch"><input type="checkbox" ${x.enabled!==false?'checked':''} onchange="setListExcludeRule(${i},'enabled',this.checked)">启用</label><input value="${esc(x.selector||'')}" onchange="setListExcludeRule(${i},'selector',this.value)" placeholder="列表项内的相对 CSS 选择器"><select onchange="setListExcludeRule(${i},'operator',this.value)">${opts([['contains','包含文字'],['equals','等于'],['not_contains','不包含'],['exists','元素出现'],['missing','元素消失'],['empty','内容为空']],x.operator||'contains')}</select><input value="${esc(x.value||'')}" onchange="setListExcludeRule(${i},'value',this.value)" placeholder="判断文字"><button onclick="pickListExcludeElement(${i})">打开浏览器 · 重选判断元素</button><button class="danger" onclick="delListExcludeRule(${i})">删除</button></div>`).join('')||'<div class="empty-rules">暂无过滤规则。选择下方数据列表和浏览器账号后，点击右上角“打开浏览器 · 新增过滤规则”。命中后只跳过当前这一条数据。</div>'}<div class="card-body grid two"><label>元素选择数据列表<select id="excludePickerSource">${r.boards.filter(x=>x.url).map(x=>`<option value="${esc(x.id)}">${esc(x.name)}</option>`).join('')}</select></label><label>浏览器账号<select id="excludePickerAccount">${pickerAccountOptions(r)}</select></label><div id="excludePickerState" class="notice" style="margin:0;grid-column:1/-1">选择数据列表和浏览器账号，再点击“打开浏览器 · 新增过滤规则”；在浏览器中点击标题、作者或分类等具体元素。</div></div></div>`}
function pickerAccountOptions(r){let mode=r.account?.mode||'independent',out=['<option value="_shared">统一账号（推荐）</option>','<option value="__picker__">游客状态（不使用登录信息）</option>'];for(const b of r.boards||[]){if(mode==='shared')continue;if(mode==='mixed'&&b.account_mode!=='independent')continue;out.push(`<option value="${esc(b.id)}">独立账号：${esc(b.name)}</option>`)}return out.join('')}
function fields(r){let ps=r.picker_sources||[];return `<div class="card"><div class="card-head"><h2>可视化规则拾取器</h2><button class="primary" onclick="openPicker()">打开浏览器 · 选择字段</button></div><div class="card-body"><div class="grid"><label>数据列表<select id="pickerSource">${ps.map((x,i)=>`<option value="${i}">${esc(x.name)}</option>`).join('')}<option value="__manual__">临时输入网页地址</option></select></label><label>浏览器账号<select id="pickerAccount">${pickerAccountOptions(r)}</select></label><label>临时网页地址<input id="pickerUrl" placeholder="https://example.com/thread-1.html"></label></div><div id="pickerState" class="notice" style="margin-top:16px;margin-bottom:0">选择页面和浏览器账号后打开拾取器；使用已登录账号时，请先关闭该账号的预登录或采集窗口。</div><div id="pickedBox"></div></div></div><div class="card"><div class="card-head"><h2>拾取器数据列表 · ${ps.length} 条</h2><div><button class="danger" onclick="deleteSelectedPickerSources()">删除所选</button><button onclick="addPickerSource()">＋ 增加来源</button></div></div>${ps.map((x,i)=>`<div class="picker-source-row"><input type="checkbox" ${selectedPickerSources.has(i)?'checked':''} onchange="togglePickerSource(${i},this.checked)"><input value="${esc(x.name)}" onchange="setPickerSource(${i},'name',this.value)" placeholder="显示名称"><input value="${esc(x.source)}" onchange="setPickerSource(${i},'source',this.value)" placeholder="sample:文件.html 或 https://网址"><button class="danger" onclick="delPickerSource(${i})">删除</button></div>`).join('')||'<div class="empty-rules">暂无预设来源，仍可临时输入网页地址。</div>'}</div><div class="card"><div class="card-head"><h2>元素提取规则 · ${r.fields.length} 条</h2><div><button class="danger" onclick="deleteSelectedRules()">删除所选</button> <button onclick="addField()">＋ 新增规则</button></div></div>${r.fields.map((f,i)=>`<div class="field rule-row"><input type="checkbox" ${selectedRules.has(i)?'checked':''} onchange="toggleRule(${i},this.checked)"><input value="${esc(f.name)}" onchange="setField(${i},'name',this.value)" placeholder="字段名称"><select onchange="setField(${i},'kind',this.value)">${['css','label_next','discuz_showhide','breadcrumb','body_after_fields'].map(k=>`<option ${f.kind===k?'selected':''}>${k}</option>`).join('')}</select><input value="${esc(f.selector||f.label||'')}" onchange="setFieldValue(${i},this.value)" placeholder="选择器或标签文字"><input value="${esc(f.attribute||'')}" onchange="setField(${i},'attribute',this.value)" placeholder="属性，如 href"><button class="danger" onclick="delField(${i})">删除</button></div>`).join('')}</div><p class="hint">数据列表和元素规则互相独立。修改名称或地址后点击顶部“保存配置”；删除操作会自动保存。</p>`}
function limits(r){let f=r.frequency,l=r.limits;return `<div class="card"><div class="card-head"><h2>访问频率</h2></div><div class="card-body grid"><label>列表间隔（秒）<input type="number" min="1" value="${f.list_seconds}" onchange="setNum('frequency.list_seconds',this.value)"></label><label>详情间隔（秒）<input type="number" min="1" value="${f.detail_seconds}" onchange="setNum('frequency.detail_seconds',this.value)"></label><label>超时（秒）<input type="number" value="${f.timeout_seconds}" onchange="setNum('frequency.timeout_seconds',this.value)"></label><label>最大重试<input type="number" value="${f.retry_limit}" onchange="setNum('frequency.retry_limit',this.value)"></label><label>错误退避（秒）<input type="number" value="${f.backoff_seconds}" onchange="setNum('frequency.backoff_seconds',this.value)"></label></div></div><div class="card"><div class="card-head"><h2>读取上限</h2></div><div class="card-body grid"><label>最大列表页<input type="number" value="${l.max_list_pages}" onchange="setNum('limits.max_list_pages',this.value)"></label><label>最大详情数<input type="number" value="${l.max_details}" onchange="setNum('limits.max_details',this.value)"></label><label>最长运行（分钟）<input type="number" value="${l.max_minutes}" onchange="setNum('limits.max_minutes',this.value)"></label><label>连续空页停止<input type="number" value="${l.empty_pages}" onchange="setNum('limits.empty_pages',this.value)"></label><label>验证码上限<input type="number" value="${l.max_captcha}" onchange="setNum('limits.max_captcha',this.value)"></label><label>连续错误上限<input type="number" value="${l.max_errors}" onchange="setNum('limits.max_errors',this.value)"></label></div></div>${stopRules(r)}`}
function opts(values,current){return values.map(([v,n])=>`<option value="${v}" ${v===current?'selected':''}>${n}</option>`).join('')}
function stopRules(r){let rules=r.stop?.rules||[],fields=r.fields||[];return `<div class="card"><div class="card-head"><h2>可视化停止条件 · ${rules.length} 条</h2><button onclick="addStopRule()">＋ 增加条件</button></div>${rules.map((x,i)=>`<div class="stop-rule"><label class="enable-switch"><input type="checkbox" ${x.enabled!==false?'checked':''} onchange="setStopRule(${i},'enabled',this.checked)">启用</label><select onchange="setStopKind(${i},this.value)">${opts([['field','已提取字段'],['element','页面元素'],['page_text','页面全文']],x.kind)}</select>${x.kind==='field'?`<select onchange="setStopRule(${i},'field',this.value)">${fields.map(f=>`<option ${f.name===x.field?'selected':''}>${esc(f.name)}</option>`).join('')}</select>`:x.kind==='element'?`<input value="${esc(x.selector||'')}" onchange="setStopRule(${i},'selector',this.value)" placeholder="CSS 选择器">`:'<span class="stop-placeholder">当前页面全文</span>'}<select onchange="setStopRule(${i},'operator',this.value)">${opts([['contains','包含文字'],['equals','等于'],['not_contains','不包含'],['exists','出现'],['missing','消失'],['empty','内容为空']],x.operator)}</select><input value="${esc(x.value||'')}" onchange="setStopRule(${i},'value',this.value)" placeholder="判断文字"><select onchange="setStopRule(${i},'scope',this.value)">${opts([['board','停止当前数据列表'],['target','停止整个目标']],x.scope)}</select>${x.kind==='element'?`<button onclick="pickStopElement(${i})">打开浏览器 · 重选停止元素</button>`:''}<button class="danger" onclick="delStopRule(${i})">删除</button></div>`).join('')||'<div class="empty-rules">暂无停止条件，达到读取上限后结束。</div>'}<div class="card-body grid two stop-picker-config"><label>元素选择页面网址<input id="stopPickerUrl" placeholder="https://example.com/detail.html"></label><label>浏览器账号<select id="stopPickerAccount">${pickerAccountOptions(r)}</select></label><button class="primary" style="grid-column:1/-1" onclick="startNewStopElementPicker()">打开浏览器 · 选择停止元素</button><div id="stopPickerState" class="notice" style="margin:0;grid-column:1/-1">填写页面网址并选择账号后点击上方按钮；在浏览器中点击用于判断停止的元素。</div></div></div>`}
function proxy(r){let p=r.proxy,c=r.captcha;return `<div class="notice warning">支持直连、固定代理和代理 API。代理配置仅用于你有权访问的网络环境；修改后请保存设置并重新启动目标。</div><div class="card"><div class="card-head"><h2>代理模式</h2></div><div class="card-body grid"><label>使用方式<select onchange="setPath('proxy.mode',this.value)"><option value="direct" ${p.mode==='direct'?'selected':''}>不使用代理</option><option value="fixed" ${p.mode==='fixed'?'selected':''}>固定代理</option><option value="api" ${p.mode==='api'?'selected':''}>API 获取代理</option></select></label><label>代理用户名<input value="${esc(p.username)}" onchange="setPath('proxy.username',this.value)"></label><label>代理密码<input type="password" value="${esc(p.password)}" onchange="setPath('proxy.password',this.value)"></label></div></div><div class="card"><div class="card-head"><h2>固定代理</h2></div><div class="card-body"><label>代理地址<input placeholder="http://host:port 或 socks5://host:port" value="${esc(p.server)}" onchange="setPath('proxy.server',this.value)"></label></div></div><div class="card"><div class="card-head"><h2>API 代理</h2></div><div class="card-body grid two"><label>API 地址<input placeholder="https://provider.example/api/get" value="${esc(p.api_url)}" onchange="setPath('proxy.api_url',this.value)"></label><label>请求方式<select onchange="setPath('proxy.api_method',this.value)"><option ${p.api_method==='GET'?'selected':''}>GET</option><option ${p.api_method==='POST'?'selected':''}>POST</option></select></label><label>JSON 取值路径<input placeholder="例如 data.proxy 或 data.0.ip" value="${esc(p.api_json_path)}" onchange="setPath('proxy.api_json_path',this.value)"></label><label>无协议时使用<select onchange="setPath('proxy.api_scheme',this.value)"><option ${p.api_scheme==='http'?'selected':''}>http</option><option ${p.api_scheme==='socks5'?'selected':''}>socks5</option></select></label><label>认证请求头名称<input placeholder="例如 Authorization" value="${esc(p.api_header_name)}" onchange="setPath('proxy.api_header_name',this.value)"></label><label>认证请求头内容<input type="password" placeholder="例如 Bearer token" value="${esc(p.api_header_value)}" onchange="setPath('proxy.api_header_value',this.value)"></label><label>获取失败重试次数<input type="number" min="0" value="${p.api_retries??2}" onchange="setNum('proxy.api_retries',this.value)"></label><label>POST 请求正文<textarea placeholder="可选，按代理服务商要求填写" onchange="setPath('proxy.api_body',this.value)">${esc(p.api_body||'')}</textarea></label></div></div><div class="card"><div class="card-head"><h2>验证码处理</h2></div><div class="card-body grid two"><label>自动打码（ddddocr）<select onchange="setPath('captcha.auto',this.value==='true')"><option value="true" ${c.auto!==false?'selected':''}>启用</option><option value="false" ${c.auto===false?'selected':''}>关闭</option></select></label><label>自动尝试次数<input type="number" min="1" value="${c.max_auto_tries??3}" onchange="setNum('captcha.max_auto_tries',this.value)"></label><label>目标专用补充文字（逗号分隔）<textarea placeholder="内置检测库未覆盖时再填写" onchange="setPath('captcha.texts',this.value.split(',').map(x=>x.trim()).filter(Boolean))">${esc((c.texts||[]).join(','))}</textarea></label><label>检测选择器（每行一个）<textarea onchange="setPath('captcha.selectors',this.value.split('\n').filter(Boolean))">${esc((c.selectors||[]).join('\n'))}</textarea></label></div><p class="hint">常见中英文验证文案和主流验证码组件已内置且自动更新，不在输入框中重复显示。这里仅补充目标网站特有文案；系统还会结合正常页面结构、页面指纹和验证组件共同判断。</p></div>`}
async function results(force=false){if(resultsBusy||!current||tab!=='results')return;resultsBusy=true;let targetId=current.id,page=resultsPage;try{const x=await api(`/api/targets/${targetId}/results?page=${page}&page_size=20`),rows=x.rows;if(!current||current.id!==targetId||tab!=='results')return;resultsPage=x.page;let signature=`${targetId}:${x.page}:${x.total}:${rows.map(v=>v.id).join(',')}`;if(!force&&signature===resultsSignature)return;resultsSignature=signature;$('#panel').innerHTML=`<div class="card"><div class="card-head"><h2>独立采集结果 · ${x.total} 条 <small style="font-weight:400">实时更新中</small></h2><div><button class="danger" onclick="clearResults()">清空</button><a href="/api/targets/${current.id}/export.csv"><button>导出 CSV</button></a></div></div><div class="table-wrap"><table><thead><tr><th>时间</th><th>数据列表</th><th>标题</th><th>列表时间</th><th>数据</th></tr></thead><tbody>${rows.map(v=>`<tr><td>${esc(v.collected_at)}</td><td>${esc(v.board_name)}</td><td>${esc(v.title)}</td><td>${esc(v.list_time)}</td><td>${esc(JSON.stringify(v.data))}</td></tr>`).join('')}</tbody></table></div><div class="result-pages"><button ${x.page<=1?'disabled':''} onclick="goResultsPage(${x.page-1})">上一页</button><span>第 ${x.page} / ${x.pages} 页 · 每页 ${x.page_size} 条 · 每 1.5 秒自动检查</span><button ${x.page>=x.pages?'disabled':''} onclick="goResultsPage(${x.page+1})">下一页</button></div></div>`}finally{resultsBusy=false}}
function goResultsPage(page){resultsPage=page;resultsSignature='';results(true)}
async function clearResults(){if(!confirm('确定清空当前目标的全部采集结果吗？此操作无法撤销。'))return;let x=await api(`/api/targets/${current.id}/results`,{method:'DELETE'});alert(`已清空 ${x.deleted} 条结果`);resultsSignature='';results(true)}
function setPath(path,v){let o=current.rule,a=path.split('.');for(let i=0;i<a.length-1;i++)o=o[a[i]];o[a.at(-1)]=v}function setNum(p,v){setPath(p,Number(v))}function setBoard(i,k,v){current.rule.boards[i][k]=v}function addBoard(){current.rule.boards.push({name:'新版块',url:'',enabled:true,proxy:''});render()}function delBoard(i){current.rule.boards.splice(i,1);render()}function setField(i,k,v){current.rule.fields[i][k]=v}function setFieldValue(i,v){let f=current.rule.fields[i];if(['label_next','discuz_showhide'].includes(f.kind))f.label=v;else f.selector=v}function addField(){current.rule.fields.push({name:`新字段${current.rule.fields.length+1}`,kind:'css',selector:''});render()}function delField(i){current.rule.fields.splice(i,1);selectedRules.clear();render()}function toggleRule(i,on){on?selectedRules.add(i):selectedRules.delete(i)}function deleteSelectedRules(){if(!selectedRules.size)return alert('请先勾选要删除的规则');current.rule.fields=current.rule.fields.filter((_,i)=>!selectedRules.has(i));selectedRules.clear();render()}
function addStopRule(){current.rule.stop=current.rule.stop||{rules:[]};current.rule.stop.rules=current.rule.stop.rules||[];current.rule.stop.rules.push({enabled:true,kind:'field',phase:'detail',field:current.rule.fields[0]?.name||'',operator:'contains',value:'',scope:'board'});render()}
function setStopRule(i,k,v){current.rule.stop.rules[i][k]=v}
function setStopKind(i,kind){let x=current.rule.stop.rules[i];x.kind=kind;x.phase='detail';if(kind==='field')x.field=x.field||current.rule.fields[0]?.name||'';if(kind==='element')x.selector=x.selector||'';render()}
function delStopRule(i){current.rule.stop.rules.splice(i,1);render()}
function addListExcludeRule(){current.rule.list.exclude_rules=current.rule.list.exclude_rules||[];current.rule.list.exclude_rules.push({enabled:true,selector:'',operator:'contains',value:''});render()}
async function startNewListExcludePicker(){let boardId=$('#excludePickerSource')?.value,account=$('#excludePickerAccount')?.value||'__picker__';current.rule.list.exclude_rules=current.rule.list.exclude_rules||[];current.rule.list.exclude_rules.push({enabled:true,selector:'',operator:'contains',value:''});let index=current.rule.list.exclude_rules.length-1;render();if(boardId)$('#excludePickerSource').value=boardId;if(account)$('#excludePickerAccount').value=account;await pickListExcludeElement(index)}
function setListExcludeRule(i,k,v){current.rule.list.exclude_rules[i][k]=v}
async function delListExcludeRule(i){current.rule.list.exclude_rules.splice(i,1);render();await persist()}
let listExcludePickerIndex=-1;
async function pickListExcludeElement(i){let boardId=$('#excludePickerSource')?.value,board=current.rule.boards.find(x=>x.id===boardId),account=$('#excludePickerAccount')?.value||'__picker__';if(!board?.url)return alert('请先选择带网址的数据列表');listExcludePickerIndex=i;await api(`/api/targets/${current.id}/picker/start`,{method:'POST',body:JSON.stringify({source:board.url,board_id:account,mode:'list_filter'})});pollListExcludePicker()}
async function pollListExcludePicker(){if(!current||tab!=='filter'||listExcludePickerIndex<0)return;let state=await api(`/api/targets/${current.id}/picker`),node=$('#excludePickerState');if(node)node.textContent=state.message||state.state;if(state.selected?.list_filter){let rule=current.rule.list.exclude_rules[listExcludePickerIndex];rule.selector=state.selected.selector;if(!rule.value)rule.value=state.selected.text;listExcludePickerIndex=-1;await api(`/api/targets/${current.id}/picker/stop`,{method:'POST'});await persist();render();return}if(['starting','running'].includes(state.state))setTimeout(pollListExcludePicker,700)}
async function pickStopElement(i){let source=$('#stopPickerUrl')?.value.trim(),board_id=$('#stopPickerAccount')?.value||'__picker__';if(!/^https?:\/\//i.test(source||''))return alert('请先填写用于选择元素的完整页面网址');stopPickerIndex=i;await api(`/api/targets/${current.id}/picker/start`,{method:'POST',body:JSON.stringify({source,board_id,mode:'stop'})});pollStopPicker()}
async function startNewStopElementPicker(){let source=$('#stopPickerUrl')?.value.trim();if(!/^https?:\/\//i.test(source||''))return alert('请先填写用于选择元素的完整页面网址');current.rule.stop.rules.push({enabled:true,kind:'element',phase:'detail',selector:'',operator:'exists',value:'',scope:'board'});await pickStopElement(current.rule.stop.rules.length-1)}
async function pollStopPicker(){if(!current||tab!=='limits'||stopPickerIndex<0)return;let state=await api(`/api/targets/${current.id}/picker`),node=$('#stopPickerState');if(node)node.textContent=state.message||state.state;if(state.selected?.stop_rule){current.rule.stop.rules[stopPickerIndex].selector=state.selected.selector;current.rule.stop.rules[stopPickerIndex].kind='element';stopPickerIndex=-1;await api(`/api/targets/${current.id}/picker/stop`,{method:'POST'});render();return}if(['starting','running'].includes(state.state))setTimeout(pollStopPicker,700)}
async function openPicker(){let source=$('#pickerSource').value;if(source==='live'||current.rule.adapter==='generic')source=$('#pickerUrl').value.trim();if(!source)return alert('请先填写以 http:// 或 https:// 开头的目标网页地址');await api(`/api/targets/${current.id}/picker/start`,{method:'POST',body:JSON.stringify({source})});updatePicker()}
async function updatePicker(){if(!current||tab!=='fields'||!$('#pickerState'))return;let s=await api(`/api/targets/${current.id}/picker`);$('#pickerState').innerHTML=`${esc(s.message||s.state)} ${s.state==='running'?`<button onclick="activatePicker()" style="margin-left:12px">窗口置前</button>`:''}`;if(s.selected){picked=s.selected;let savedFields=picked.saved_fields||[];if(picked.saved&&!savedFields.length&&picked.name)savedFields=[{name:picked.name,kind:'css',selector:picked.selector,...(picked.attribute?{attribute:picked.attribute}:{})}];savedFields.forEach(field=>{if(!current.rule.fields.some(f=>f.name===field.name&&f.selector===field.selector))current.rule.fields.push(field)});$('#pickedBox').innerHTML=picked.saved?`<div class="picker-result"><div><small>批量规则已保存</small><b>${savedFields.map(x=>esc(x.name)).join('、')}</b></div><span>✓ 已写入 JSON</span></div>`:`<div class="picker-result"><div><small>已选文本</small><b>${esc(picked.text||'(无文本)')}</b><code>${esc(picked.selector||'')}</code></div><button class="primary" onclick="addPickedField()">添加为字段</button></div>`}}
async function activatePicker(){await api(`/api/targets/${current.id}/picker/activate`,{method:'POST'})}
function addPickedField(){if(!picked)return;let name=prompt('字段名称，例如：详细地址');if(!name)return;current.rule.fields.push({name,kind:'css',selector:picked.selector,...(picked.attribute?{attribute:picked.attribute}:{})});render()}
$('#saveBtn').onclick=async()=>{await api('/api/targets/'+current.id,{method:'PUT',body:JSON.stringify(current)});$('#saveBtn').textContent='已保存';setTimeout(()=>$('#saveBtn').textContent='保存设置',1200)}
$('#ruleBtn').onclick=()=>{if(current)location.href=`/api/targets/${current.id}/rule.json`}
$('#startBtn').onclick=async()=>{if(!current)return;await persist();updateStatus(await api(`/api/targets/${current.id}/actions/start`,{method:'POST'}))}
$('#pauseBtn').onclick=async()=>{if(!current)return;let s=await api(`/api/targets/${current.id}/status`),action=s.state==='paused'?'resume':'pause';updateStatus(await api(`/api/targets/${current.id}/actions/${action}`,{method:'POST'}))}
$('#stopBtn').onclick=async()=>{if(!current)return;if(!confirm('确定停止当前任务吗？已采集结果会保留。'))return;updateStatus(await api(`/api/targets/${current.id}/actions/stop`,{method:'POST'}))}
function testProxy(){alert('保存配置后，启动任务会使用此固定代理。连通性与出口 IP 检测将在下一版加入。')}
poll=setInterval(async()=>{if(current){let s=await api(`/api/targets/${current.id}/status`);updateStatus(s);updatePicker();if(tab==='results')results()}await refreshTargetStates()},1500);loadTargets();
$('#newTarget').onclick=async()=>{let name=prompt('目标名称，例如：目标3');if(!name)return;let domain=prompt('网站域名，例如 example.com')||'';let id='target_'+Date.now();await api('/api/targets',{method:'POST',body:JSON.stringify({id,name,folder:name,domain})});current=null;await loadTargets();await loadTarget(id)}

async function renameCurrentTarget(){
  if(!current)return;
  let name=prompt('请输入新的目标名称',current.name);
  if(name===null)return;
  name=name.trim();
  if(!name)return alert('目标名称不能为空');
  if(name.length>80)return alert('目标名称不能超过 80 个字符');
  if(name===current.name)return;
  current.name=name;await persist();$('#title').textContent=name;await loadTargets();
}

// 数据列表页使用独立选择状态；以下定义覆盖旧版的简单增删函数。
async function persist(){await api(`/api/targets/${current.id}`,{method:'PUT',body:JSON.stringify(current)})}
async function addBoard(){current.rule.boards.push({name:'新数据列表',url:'',enabled:true,proxy:''});render();await persist()}
async function delBoard(i){if(!confirm(`确定删除数据列表“${current.rule.boards[i].name||'未命名'}”吗？`))return;current.rule.boards.splice(i,1);selectedBoards.clear();render();await persist()}
function toggleBoard(i,on){on?selectedBoards.add(i):selectedBoards.delete(i)}
async function deleteSelectedBoards(){if(!selectedBoards.size)return alert('请先勾选要删除的数据列表页');if(!confirm(`确定删除选中的 ${selectedBoards.size} 条数据列表页吗？`))return;current.rule.boards=current.rule.boards.filter((_,i)=>!selectedBoards.has(i));selectedBoards.clear();render();await persist()}

function setPickerSource(i,k,v){current.rule.picker_sources[i][k]=v}
function togglePickerSource(i,on){on?selectedPickerSources.add(i):selectedPickerSources.delete(i)}
async function addPickerSource(){let name=prompt('列表名称，例如：帖子样例3');if(!name)return;let source=prompt('输入本地样例（如 sample:内容3.html）或完整网页地址');if(!source)return;current.rule.picker_sources=current.rule.picker_sources||[];current.rule.picker_sources.push({name,source});render();await persist()}
async function addLocalPickerSource(){let file=prompt('输入当前目标文件夹内的 HTML 文件名，例如：内容3.html');if(!file)return;file=file.trim().replace(/^sample:/,'');if(!/\.html?$/i.test(file))return alert('请输入以 .html 或 .htm 结尾的文件名');let name=prompt('显示名称',`本地样例：${file}`);if(!name)return;await appendPickerSource(name,`sample:${file}`)}
async function addOnlinePickerSource(){let url=prompt('输入完整网页地址，例如：https://example.com/thread-1.html');if(!url)return;url=url.trim();if(!/^https?:\/\//i.test(url))return alert('网页地址必须以 http:// 或 https:// 开头');let name=prompt('显示名称',new URL(url).hostname);if(!name)return;await appendPickerSource(name,url)}
async function appendPickerSource(name,source){current.rule.picker_sources=current.rule.picker_sources||[];current.rule.picker_sources.push({name,source});render();await persist()}
async function delPickerSource(i){let x=current.rule.picker_sources[i];if(!confirm(`确定删除拾取器数据列表“${x.name}”吗？`))return;current.rule.picker_sources.splice(i,1);selectedPickerSources.clear();render();await persist()}
async function deleteSelectedPickerSources(){if(!selectedPickerSources.size)return alert('请先勾选要删除的拾取器数据列表');if(!confirm(`确定删除选中的 ${selectedPickerSources.size} 个拾取器数据列表吗？`))return;current.rule.picker_sources=current.rule.picker_sources.filter((_,i)=>!selectedPickerSources.has(i));selectedPickerSources.clear();render();await persist()}

// 使用当前目标自己维护的拾取器数据列表。
async function openPicker(){let source=$('#pickerUrl')?.value.trim(),board_id=$('#pickerAccount')?.value||'__picker__';if(!/^https?:\/\//i.test(source||''))return alert('请填写以 http:// 或 https:// 开头的内容页网址');await api(`/api/targets/${current.id}/picker/start`,{method:'POST',body:JSON.stringify({source,board_id})});updatePicker()}

function addSelectAllButton(titlePrefix,total,selected,handler){
  let heading=[...document.querySelectorAll('.card-head h2')].find(x=>x.textContent.startsWith(titlePrefix));
  if(!heading)return;let actions=heading.parentElement.querySelector('.card-head>div');if(!actions)return;
  let button=document.createElement('button');button.className='select-all';button.textContent=total>0&&selected===total?'取消全选':'全选';button.onclick=handler;actions.prepend(button);
}
function decorateSelectionControls(){
  if(tab==='filter')decorateListPicker();
  if(tab==='boards')addSelectAllButton('数据列表页',current.rule.boards.length,selectedBoards.size,toggleAllBoards);
  if(tab==='fields'){
    decorateContentUrlInput();
    removePickerSourcesCard();
    decorateFieldExclusions();
    addSelectAllButton('元素提取规则',current.rule.fields.length,selectedRules.size,toggleAllRules);
  }
  decorateOrderingControls();
  if(tab==='boards')removeSourceProxyInputs();
  removeRedundantNotices();
}
function decorateFieldExclusions(){document.querySelectorAll('.field.rule-row').forEach((row,i)=>{let inputs=row.querySelectorAll(':scope>input'),attribute=inputs[inputs.length-1];if(!attribute||row.querySelector('.field-exclude'))return;let input=document.createElement('input');input.className='field-exclude';input.value=(current.rule.fields[i].exclude_contains||[]).join(', ');input.placeholder='字段包含即不采集';input.onchange=()=>current.rule.fields[i].exclude_contains=input.value.split(',').map(v=>v.trim()).filter(Boolean);row.insertBefore(input,attribute)})}
function decorateContentUrlInput(){let source=$('#pickerSource'),input=$('#pickerUrl');source?.closest('label')?.remove();if(!input)return;let label=input.closest('label'),text=[...label.childNodes].find(x=>x.nodeType===Node.TEXT_NODE);if(text)text.textContent='内容页网址';input.placeholder='https://example.com/thread-1.html';label.parentElement?.classList.add('two');let state=$('#pickerState');if(state)state.textContent='填写内容页网址并选择浏览器账号后打开拾取器；使用已登录账号时，请先关闭该账号的预登录或采集窗口。'}
function removePickerSourcesCard(){let heading=[...document.querySelectorAll('.card-head h2')].find(x=>x.textContent.startsWith('拾取器数据列表'));heading?.closest('.card')?.remove()}
function decorateListPicker(){
  let card=document.createElement('div');card.className='card';card.innerHTML=`<div class="card-head"><h2>列表规则识别</h2><div><button onclick="autoListPicker()">后台识别并保存规则</button><button class="primary" onclick="openListPicker()">打开浏览器 · 修正列表规则</button></div></div><div class="card-body grid two"><label>选择数据列表页<select id="listPickerSource">${current.rule.boards.filter(x=>x.url).map(x=>`<option value="${esc(x.id)}">${esc(x.name)}</option>`).join('')}</select></label><label>浏览器账号<select id="listPickerAccount">${pickerAccountOptions(current.rule)}</select></label><div id="listPickerState" class="notice" style="margin:0;grid-column:1/-1">自动识别确认后直接保存；手动修正会先载入当前规则，只需重选不准确的项目。</div></div>`;$('#panel').prepend(card);renameFilterLabels()
}
function renameFilterLabels(){let names={'帖子行 CSS 选择器':'列表项容器 CSS 选择器','详情链接 CSS 选择器':'详情入口 CSS 选择器','下一页 CSS 选择器':'下一页/加载更多 CSS 选择器'};document.querySelectorAll('#panel label').forEach(label=>{let node=[...label.childNodes].find(x=>x.nodeType===Node.TEXT_NODE&&x.textContent.trim());if(node&&names[node.textContent.trim()])node.textContent=names[node.textContent.trim()]})}
function listPickerRequest(){let id=$('#listPickerSource')?.value,board=current.rule.boards.find(x=>x.id===id),board_id=$('#listPickerAccount')?.value||'__picker__';return {source:board?.url||'',board_id}}
async function openListPicker(){let request=listPickerRequest();if(!request.source)return alert('请先添加带网址的数据列表');await api(`/api/targets/${current.id}/picker/start`,{method:'POST',body:JSON.stringify({...request,mode:'list'})});pollListPicker()}
async function autoListPicker(){let request=listPickerRequest();if(!request.source)return alert('请先添加带网址的数据列表');$('#listPickerState').textContent='正在使用所选浏览器账号分析页面结构…';await api(`/api/targets/${current.id}/picker/start`,{method:'POST',body:JSON.stringify({...request,mode:'list_auto'})});pollAutoListPicker()}
async function pollAutoListPicker(){if(!current||tab!=='filter'||!$('#listPickerState'))return;let state=await api(`/api/targets/${current.id}/picker`);$('#listPickerState').textContent=state.message||state.state;if(['starting','running'].includes(state.state)){setTimeout(pollAutoListPicker,700);return}let x=state.selected;if(!x?.ok)return alert(x?.message||state.message||'自动识别失败');let preview=(x.previews||[]).map(v=>`• ${v.text}　[时间：${v.time||'未识别'}]`).join('\n');let message=`识别到 ${x.row_count} 个列表项\n\n列表项：${x.row_selector}\n详情入口：${x.link_selector}\n列表时间：${x.time_selector||'未识别'}\n翻页方式：${x.pagination_mode==='scroll'?'滚动到底加载':'下一页按钮'}\n翻页：${x.next_selector||'滚动模式无需按钮'}\n\n示例：\n${preview}\n\n确认覆盖当前列表定位规则吗？`;if(!confirm(message))return;if(!x.time_selector||(x.pagination_mode!=='scroll'&&!x.next_selector))return alert('时间或翻页控件未识别，请使用手动选择修正');for(let key of ['row_selector','link_selector','time_selector','next_selector','pagination_mode'])current.rule.list[key]=x[key];await persist();render()}
async function pollListPicker(){if(!current||tab!=='filter'||!$('#listPickerState'))return;let state=await api(`/api/targets/${current.id}/picker`);$('#listPickerState').textContent=state.message||state.state;if(state.selected?.saved){for(let key of ['row_selector','link_selector','time_selector','next_selector','pagination_mode'])current.rule.list[key]=state.selected[key];await api(`/api/targets/${current.id}/picker/stop`,{method:'POST'});render();return}if(['starting','running'].includes(state.state))setTimeout(pollListPicker,800)}
function removeSourceProxyInputs(){document.querySelectorAll('.source-rule').forEach(row=>{let inputs=row.querySelectorAll(':scope>input');if(inputs.length>=4)inputs[3].remove()})}
function removeRedundantNotices(){
  let prefixes=[
    '这里每一行都是一条数据列表页。',
    '支持直连、固定代理和代理 API。',
    '运行模式在下次启动任务时生效。',
    '过滤发生在列表页。当前规则：'
  ];
  document.querySelectorAll('#panel .notice').forEach(node=>{
    if(prefixes.some(prefix=>node.textContent.trim().startsWith(prefix)))node.remove();
  });
}
function decorateOrderingControls(){
  let groups=[];
  if(tab==='boards')groups.push(['boards',[...document.querySelectorAll('.source-rule')]]);
  if(tab==='fields')groups.push(['picker_sources',[...document.querySelectorAll('.picker-source-row')]],['fields',[...document.querySelectorAll('.rule-row')]]);
  groups.forEach(([kind,rows])=>rows.forEach((row,index)=>{
    let box=document.createElement('div');box.className='order-buttons';
    let up=document.createElement('button');up.type='button';up.textContent='↑';up.title='上移';up.disabled=index===0;up.onclick=()=>moveRuleItem(kind,index,-1);
    let down=document.createElement('button');down.type='button';down.textContent='↓';down.title='下移';down.disabled=index===rows.length-1;down.onclick=()=>moveRuleItem(kind,index,1);
    box.append(up,down);row.insertBefore(box,row.lastElementChild);
  }));
}
async function moveRuleItem(kind,index,direction){
  let list=current.rule[kind]||[],next=index+direction;if(next<0||next>=list.length)return;
  [list[index],list[next]]=[list[next],list[index]];
  selectedBoards.clear();selectedPickerSources.clear();selectedRules.clear();render();await persist();
}
function decoratePickerSourceButtons(){
  let heading=[...document.querySelectorAll('.card-head h2')].find(x=>x.textContent.startsWith('拾取器数据列表'));
  if(!heading)return;let actions=heading.parentElement.querySelector('.card-head>div');if(!actions)return;
  let old=actions.querySelector('[onclick="addPickerSource()"]');
  if(old){old.textContent='＋ 添加在线网页';old.removeAttribute('onclick');old.onclick=addOnlinePickerSource}
  if(!actions.querySelector('.add-local-picker')){let button=document.createElement('button');button.className='add-local-picker';button.textContent='＋ 添加本地 HTML';button.onclick=addLocalPickerSource;actions.insertBefore(button,old)}
}
function toggleAllBoards(){let n=current.rule.boards.length;selectedBoards=selectedBoards.size===n&&n?new Set():new Set(current.rule.boards.map((_,i)=>i));render()}
function toggleAllPickerSources(){let a=current.rule.picker_sources||[],n=a.length;selectedPickerSources=selectedPickerSources.size===n&&n?new Set():new Set(a.map((_,i)=>i));render()}
function toggleAllRules(){let n=current.rule.fields.length;selectedRules=selectedRules.size===n&&n?new Set():new Set(current.rule.fields.map((_,i)=>i));render()}

async function deleteCurrentTarget(){
  if(!current)return;
  let name=current.name,id=current.id;
  if(!confirm(`确定删除目标“${name}”吗？\n\n目标配置、采集结果和事件记录会删除；原始 HTML 文件夹会保留。`))return;
  await api(`/api/targets/${id}`,{method:'DELETE'});
  current=null;selectedRules.clear();selectedBoards.clear();selectedPickerSources.clear();
  $('#panel').innerHTML='';$('#title').textContent='请选择或新建目标';$('#crumb').textContent='—';
  await loadTargets();
}

// 元素规则删除后立即写入目标配置，避免刷新后恢复。
async function delField(i){
  let field=current.rule.fields[i];
  if(!field)return;
  if(!confirm(`确定删除元素提取规则“${field.name||'未命名'}”吗？`))return;
  current.rule.fields.splice(i,1);selectedRules.clear();render();await persist();
}
async function deleteSelectedRules(){
  if(!selectedRules.size)return alert('请先勾选要删除的元素提取规则');
  if(!confirm(`确定删除选中的 ${selectedRules.size} 条元素提取规则吗？`))return;
  current.rule.fields=current.rule.fields.filter((_,i)=>!selectedRules.has(i));
  selectedRules.clear();render();await persist();
}

function browserMode(r){let mode=r.browser?.mode||'visible';return `<div class="notice">运行模式在下次启动任务时生效。自动切换模式会在正常采集时最小化窗口，检测到验证码时自动恢复。</div><div class="mode-cards"><label class="mode-card"><input type="radio" name="browserMode" value="visible" ${mode==='visible'?'checked':''} onchange="setPath('browser.mode',this.value)"><b>可视运行</b><span>始终显示独立浏览器，适合首次登录和调试。</span></label><label class="mode-card"><input type="radio" name="browserMode" value="silent" ${mode==='silent'?'checked':''} onchange="setPath('browser.mode',this.value)"><b>后台静默</b><span>无窗口运行、资源占用较低；验证码出现时需停止并切换模式。</span></label><label class="mode-card"><input type="radio" name="browserMode" value="auto" ${mode==='auto'?'checked':''} onchange="setPath('browser.mode',this.value)"><b>自动切换</b><span>平时最小化；验证码时恢复窗口，处理完成后再次最小化。</span></label></div><p class="hint">选择后点击顶部“保存设置”，再开始运行。</p>`}

// 每条数据列表拥有稳定 ID，并绑定独立账号目录。
async function addBoard(){current.rule.boards.push({id:`source_${Date.now()}_${Math.random().toString(16).slice(2,8)}`,name:'新数据列表',url:'',enabled:true,proxy:'',account_mode:'shared'});render();await persist()}
$('#loginBtn').onclick=async()=>{
  if(!current)return;await persist();let accounts=await api(`/api/targets/${current.id}/accounts`);
  if(Array.isArray(accounts))accounts={mode:current.rule.account?.mode||'independent',shared_saved:false,sources:accounts};
  let mode=accounts.mode||'independent';
  let modal=$('#dialog');modal.querySelector('.dialog-head h2').textContent='账号/代理管理';
  let strategy=`<div class="card"><div class="card-head"><h2>账号使用策略</h2></div><div class="card-body grid two"><label>登录方式<select onchange="setAccountMode(this.value)"><option value="shared" ${mode==='shared'?'selected':''}>所有数据列表使用统一账号</option><option value="independent" ${mode==='independent'?'selected':''}>每个数据列表使用独立账号</option><option value="mixed" ${mode==='mixed'?'selected':''}>混合模式</option></select></label><div><button class="primary" onclick="startSourceLogin('_shared')">${accounts.shared_saved?'打开浏览器 · 更换统一账号':'打开浏览器 · 登录统一账号'}</button><p class="hint">统一账号只需登录一次；混合模式中选择“继承统一账号”的数据列表也使用该登录状态。</p></div></div></div>`;
  let rows=accounts.sources||[];
  $('#dialogBody').innerHTML=strategy+(rows.length?`<div class="table-wrap"><table><thead><tr><th>数据列表</th><th>账号方式</th><th>登录状态</th><th>独立固定代理</th><th>操作</th></tr></thead><tbody>${rows.map(x=>{let b=current.rule.boards.find(v=>v.id===x.id)||{},effectiveShared=x.profile_id==='_shared'||mode==='shared'||(mode==='mixed'&&b.account_mode!=='independent');return `<tr><td>${esc(x.name)}</td><td>${mode==='mixed'?`<select onchange="setSourceAccountMode('${x.id}',this.value)"><option value="shared" ${b.account_mode!=='independent'?'selected':''}>继承统一账号</option><option value="independent" ${b.account_mode==='independent'?'selected':''}>使用独立账号</option></select>`:(mode==='shared'?'统一账号':'独立账号')}</td><td>${x.saved?(effectiveShared?'统一账号已保存':'独立账号已保存'):(effectiveShared?'统一账号未登录':'独立账号未登录')}</td><td><input class="account-proxy" value="${esc(b.proxy||'')}" placeholder="留空继承目标代理" onchange="setSourceProxy('${x.id}',this.value)"></td><td>${effectiveShared?'<span class="hint">使用统一登录</span>':`<button class="primary" onclick="startSourceLogin('${x.id}')">${x.saved?'打开浏览器 · 更换账号':'打开浏览器 · 登录账号'}</button>`}</td></tr>`}).join('')}</tbody></table><p class="hint">账号和代理分别继承：统一账号不强制所有数据列表使用同一代理。独立代理留空时继承目标代理设置。</p></div>`:'<div class="empty-rules">请先添加数据列表页。</div>');
  modal.showModal();
}
async function startSourceLogin(boardId){let state=await api(`/api/targets/${current.id}/login/start`,{method:'POST',body:JSON.stringify({board_id:boardId})});if(state.state==='error')return alert(state.message);$('#dialog').close()}
async function setAccountMode(value){current.rule.account={...(current.rule.account||{}),mode:value};await persist();$('#dialog').close();$('#loginBtn').click()}
async function setSourceAccountMode(boardId,value){let board=current.rule.boards.find(x=>x.id===boardId);if(!board)return;board.account_mode=value;await persist();$('#dialog').close();$('#loginBtn').click()}
async function setSourceProxy(boardId,value){let board=current.rule.boards.find(x=>x.id===boardId);if(!board)return;board.proxy=value.trim();await persist()}
